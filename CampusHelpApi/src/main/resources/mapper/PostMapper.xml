<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yqn.mapper.PostMapper">

    <resultMap id="postResultMap" type="com.yqn.pojo.Post">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="school_id" property="schoolId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="like_count" property="likeCount"/>
        <result column="create_time" property="createTime"/>
        <result column="img_url" property="imgUrl"/>
        <result column="isLiked" property="isLiked"/>
        <association property="publisher" column="user_id" select="findUserWithAssociation"/>
    </resultMap>

    <select id="findUserWithAssociation" resultMap="com.yqn.mapper.UserMapper.userResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="selectPostListWithLike" resultMap="postResultMap">
        SELECT
            p.*,
            (CASE WHEN lr.id IS NOT NULL THEN 1 ELSE 0 END) AS isLiked
        FROM post p
                 LEFT JOIN user_like lr
                           ON p.id = lr.target_id
                               AND lr.target_type = 1
                               AND lr.user_id = #{userId}
        WHERE p.school_id = #{schoolId}
        <if test="userId != null">
            AND p.user_id = #{userId}
        </if>
        ORDER BY p.create_time DESC
    </select>

</mapper>
